<div class="container">
  <h1>Delete A Note</h1>
     <form action='/delete/<%=@note.id%>' method='post'> 
  <!--     web browsers do not support put or delete, action is set to post
            then the hidden _method which we set to delete allows us to fake
            the delete request -->
      <%=@note.note%> <input type="submit" onclick=
      "alert('Are you sure you want to delete this?')" name="_method" value="delete">
    </form>
  </div>